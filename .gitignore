# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (NEVER commit real secrets)
.env
.env*.local
.env.development
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# supabase
/supabase/.branches
/supabase/.temp

# editor
.idea
.vscode/*
!.vscode/extensions.json

# Backups written by scripts/dedupe-leads.mjs
leads-backup-*.json

# Deploy builds into .next-build and keeps the last good build as
# .next-previous so a rollback needs no rebuild. Neither belongs in git.
.next-build/
.next-previous/
